import app from './app';
import common from './common';
import error from './error';
import login from './login';

export const locale = {
  error,
  login,
  common,
  app,
};
export default locale;
